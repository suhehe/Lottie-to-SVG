{
  "name": "lottie-compiler",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "lottie-web": "^5.12.2"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.31.2",
    "vite": "^7.1.10",
    "wrangler": "^4.81.1"
  }
}
